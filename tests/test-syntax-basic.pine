//@version=6
indicator("Basic Syntax Test", overlay=true)

// Test basic syntax highlighting
var int counter = 0
var float sum = 0.0
var bool flag = true
var string message = "Hello"
var color myColor = color.blue

// Control flow
if close > open
    counter := counter + 1
else
    counter := counter - 1

// Loops
for i = 0 to 10
    sum := sum + i

while counter < 100
    counter := counter + 1

// Function
myFunction(x, y) =>
    x + y

result = myFunction(10, 20)

plot(result, title="Result", color=myColor)

